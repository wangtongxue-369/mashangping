# 计划10 · 前端 UI/UX 审查整改（P0~P3）实现计划

- 日期：2026-09-06
- 依据 spec：`docs/superpowers/specs/2026-09-06-计划10-前端UIUX审查整改-design.md`
- 审查来源：`docs/2026-09-06-前端-UIUX-审查报告.md`

## 全局约束（每任务必须遵守）

- Java 17；不用 JDK21 API（无 `List.getFirst`）。
- Flyway：仅新增 `V7__judging_actual_output.sql`（spec §7.2 已论证），不改 V1~V6。
- 零泄漏红线：学生视图隐藏点内容（输入/输出/数量口径维持现状）不得新增泄露；样例点可展示「你的输出」。
- 越权 40400；教师端点 `@PreAuthorize('hasRole('TEACHER')')`。
- 前端 tsc strict；文案中文；测试先行（先写失败测试确认 FAIL → 实现 → PASS → 全量回归 → commit）。
- commit 一次一任务：`type(scope): 中文描述`；文档改动 `docs(plan): …`。
- 无全局 mvn：`.tools/maven/bin/mvn -f server/pom.xml -Dmaven.repo.local=/Users/wangtongxue/文件/project/mashangping/.tools/m2repo`（测试需加 `-Dtree-sitter-lib` 由 surefire argLine 处理）；前端 `npm test` / `npx tsc -b` / `npm run build`。
- CLAUDE.md 的 ` M CLAUDE.md` 为用户未提交改动，不纳入任何 commit。

## Task 1 · 文档

- [x] spec/plan 写入 `docs/superpowers/{specs,plans}/2026-09-06-计划10-前端UIUX审查整改-*`
- [x] `git add docs/superpowers && git commit -m "docs(plan): 计划10 前端UIUX审查整改——spec(全量P0-P3契约)+plan(Task1-10 TDD)"`

## Task 2 · 后端：学生作业上下文 header + TeacherDetail.courseName（spec §3.3）

- [ ] 失败测试先行：
  - `AssignmentViews` 增 `record StudentAssignmentHeader(long courseId, String courseName, long assignmentId, String title, String status, LocalDateTime startAt, LocalDateTime dueAt, int lateDays, long problemCount)`；`TeacherDetail` 增 `String courseName`。
  - `StudentAssignmentService.header(uid, assignmentId)`：published + assertEnrolled(assignment.courseId)；problemCount 统计；courseName 经 CourseMapper。
  - `AssignmentController.detail` 角色分流（STUDENT 分支调用 header）。
  - IT：学生可拿 header（课程名/题数/状态正确）；未选课 40400；未发布 40400；教师分支原样 TeacherDetail(带 courseName)。
  - SchemaTest 无涉及（无表结构变更）。受影响位置性构造调用点同步（搜索 `new AssignmentViews.TeacherDetail(` / 测试断言）。
- [ ] 实现 → 单测/IT 绿 → `mvn test` 全量回归
- [ ] commit `feat(assignment): 学生作业上下文 header(课程名/状态/题数)+教师详情补 courseName，GET /assignments/{id} 角色分流`

## Task 3 · 后端：判题明细 actualOutput + 教师隐藏点完整视图（spec §5.3，唯一 V7 迁移）

- [ ] 失败测试先行：
  - `V7__judging_actual_output.sql`：`ALTER TABLE judge_detail ADD COLUMN actual_output MEDIUMTEXT NULL`。
  - `JudgeDetail` 实体 + `PointOutcome` 增 actualOutput；`DockerJudgeExecutor.judgePoint` WA 时带 `tailLines(r.stdout(),2000)`；`JudgeScheduler` 落库。
  - `StudentSubmissionViews.SamplePoint` 增 `actualOutput`（NON_NULL 时缺省）；`StudentReadService.detail` 样例点填充。
  - gradebook `TeacherSubmissionRow.SamplePoint` 增 actualOutput；`MaskedPoint` 扩展为完整视图 `(pointIndex,status,timeUsedMs,memoryUsedMb,input,expectedOutput,message,actualOutput)`（教师属主全量）；`GradebookService.submissionDetail` 填充。
  - SchemaTest（judge_detail 实体↔DDL）与受影响装配测试更新。
- [ ] 实现 → 单测/IT 绿（mock 层验证 actual 写入与读侧）→ `mvn test` 全量回归
- [ ] commit `feat(judging): judge_detail 落库实际输出——样例点下发「你的输出」，教师隐藏点完整诊断(V7)`

## Task 4 · 前端基建：注册闭环 + ADMIN 提示（spec §3.1/3.2）

- [ ] 失败测试先行：
  - `useAuth.login` 返回 `AuthUser`（调用处签名更新）。
  - `api/register.ts`：`sendRegisterCode(email)`、`register(payload)` 返回 `{token,user}`。
  - `pages/RegisterPage.tsx`（表单+倒计时+成功跳 `/student/courses`）；`App.tsx` 加 `/register`；`LoginPage` 加注册入口 + ADMIN 登录提示并清会话。
  - 用例：注册成功跳转；验证码按钮倒计时；后端 400xx 文案展示；ADMIN 提示并停留。
- [ ] 实现 → `tsc -b`/`npm test` 绿 → commit `feat(frontend): 学生自助注册闭环(邮箱验证码页+登录入口)——ADMIN登录提示防死胡同`

## Task 5 · 学生端：名称上下文 + 状态贯穿 + 工作区语义（spec §3.3/§4）

- [ ] 失败测试先行（逐页）：
  - `CourseAssignmentsPage`：课程名页眉（`GET /courses/my` 取到）、查询失败错误卡/重试、courseId 变化重置 page。
  - `AssignmentProblemsPage`：拉 header 显「课程名 / 作业名 + 状态徽标」、CLOSED/LATE_WINDOW Alert、倒计时。
  - `CodingPage`：删原始 ID、名称上下文、把 assignmentStatus/题分/标题传给工作区。
  - `CodingWorkspace` 新 props：`assignmentMeta?: {status,dueAt,lateDays,fullScore}`：CLOSED 禁提交+tooltip；LATE_WINDOW 提交 Modal.confirm；作业/练习得分语义（练习显「已通过 N 次」、作业按满分/0/部分配色）；语言按语言分存编辑内容；空/模板提交确认；历史 isLate Tag + 错误/空态区分 + 重试；`types.ts`/`studentTypes.ts` 增 header 类型。
- [ ] 实现 → `tsc -b`/`npm test`/`build` 绿 → 分 2 个 commit：
  - `feat(frontend): 学生端课程/作业名称上下文与状态贯穿——CLOSED禁提交/LATE_WINDOW迟交确认/倒计时`
  - `feat(frontend): 作答区得分语义修正(练习已通过N次/作业满分配色)+历史迟交/错误空态/语言分存`

## Task 6 · 教师端：页头组件 + 面包屑返回链 + 名称上下文（spec §4.4/§5.1 前半）

- [ ] 失败测试先行：
  - 新建 `components/PageHeader.tsx`（back/sub/title/extra 受控渲染）。
  - `AssignmentsPage` 补返回/面包屑；`CourseStudentsPage` 返回改回课程工作台；`CourseConsolePage` 面包屑与课程名（课程详情已有 `GET /courses/{id}`）；`AssignmentDetailPage` 页头用组件（保留「课程工作台」互链）；`SubmissionsPage/GradebookPage` 页头一致化含课程名；`ProblemDetailPage` 来路返回（location.state）。
  - 用例按页断言面包屑/返回目标；旧返回文案变化的测试同步更新。
- [ ] 实现 → `tsc -b`/`npm test` 绿 → commit `feat(frontend): 教师端三层面包屑与返回链统一(课程工作台为枢纽)——ProblemDetail保留来路`

## Task 7 · 工作台错误态 + 查重阈值防抖（spec §4.2 / §6）

- [ ] 失败测试先行：
  - `CourseConsolePage`：任一会话查询失败 → Alert+重试；成功卡不受影响。
  - `PlagiarismPage`：阈值本地 state + 「应用阈值」才触发重查。
- [ ] 实现 → `npm test` 绿 → commit `feat(frontend): 课程工作台错误态Alert/重试——查重阈值显式应用防抖`

## Task 8 · 教师端共享常量收敛（spec §5.1）

- [ ] 教师端状态 meta 抽公共（复用学生端已收敛常量或升共享），删除三页重复定义；颜色口径统一（测试断言同步）。
- [ ] `npm test`/`tsc` 绿 → commit `refactor(frontend): 教师端状态徽标映射收敛至共享常量(与学生端同源防漂移)`

## Task 9 · 便利功能：Excel 模板 + PENDING 说明 + 成绩册冻结列/未提交筛选 + CSV 作业名（spec §5.2）

- [ ] 失败测试先行：
  - `CourseStudentsPage`：下载模板（学号,姓名 BOM CSV）+ 上传旁格式提示 + PENDING tooltip。
  - `GradebookPage`：学号/姓名列固定左 + 「只看未提交」筛选（无任何 cell 记录的学生）。
  - 后端 `GradebookController.csv` Content-Disposition `filename*=UTF-8''gradebook-{作业名}.csv`（安全截断/替换非法字符），前端解析文件名（失败回退现名）。
- [ ] 实现 → 前后端各自回归绿 → commit `feat(gradebook)+feat(frontend): CSV带作业名/成绩册列冻结与未提交筛选——学生名单Excel模板与PENDING说明`

## Task 10 · 学生端/教师端杂项收尾（spec §6）

- [ ] 教师端作业详情题目行上移/下移重排（后端 `PUT /api/assignments/{id}/problems/order` + 前端按钮，TDD）。
- [ ] 课程/作业删除后果文案核对后端实际行为后强化；发布中作业编辑提示「改动即时对学生可见」。
- [ ] 选题 Modal 已选题目过滤与 total 口径提示（或本地分页，实现时选最小正确方案）。
- [ ] 学生/教师列表跨实体分页重置（courseId/assignmentId/筛选变化置 page=1）。
- [ ] commit：`feat(assignment): 作业题序上移下移重排`、`feat(frontend): 删除后果文案/编辑即时生效提示/选题Modal口径说明`、`fix(frontend): 列表分页随课程/作业/筛选切换重置`

## Task 11 · 全量回归 + live 实测 + 收尾

- [ ] 后端 `mvn test` 全绿；前端 `npx tsc -b && npm test && npm run build` 全绿。
- [ ] live：后端 8080 重启（`-Dtree-sitter-lib` 与 judge flag 按需）→ 冒烟：教师端面包屑互链；学生深链上下文；注册新学号自动激活（mock 邮件从日志取码）；CLOSED/LATE_WINDOW 表现（可临时把演示作业 due 拨近验证再还原）。
- [ ] 更新 `docs/2026-09-06-前端-UIUX-审查报告.md` 或追加「整改后记」小节列明已修复/未纳入，方便复核。
- [ ] commit `docs(plan): 计划10 整改后记——已修复清单与遗留项`

## 风险与后备

- 前端写代理在本环境多次停滞 → 各页改动以主代直接实施为主，必要时单文件小委派。
- 分页重置涉及多页且测试多 → 每页独立小步改 + 回归，避免一次性大面积 diff。
- V7 迁移在已有开发库上执行一次；`mvn test` 的 Testcontainers 每次新建库会自动跑迁移。
